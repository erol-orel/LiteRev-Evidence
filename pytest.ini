[pytest]
testpaths = tests
# Put the repo root on sys.path so `import main` works whether pytest is invoked
# as `pytest` (CI) or `python -m pytest` (which also adds CWD). Requires pytest>=7.
pythonpath = .
addopts = -q
